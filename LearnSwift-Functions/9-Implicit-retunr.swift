// Write your code below ➗

func findRemainder(dividend: Int, divisor: Int) -> Int {

  // se puede omitir el "return" y el programa seguiria entregando este 
  // resultado, aunque no es reomendable pues puede dificultar
  // la lectura
   dividend % divisor
}

print(findRemainder(dividend: 10, divisor: 4))